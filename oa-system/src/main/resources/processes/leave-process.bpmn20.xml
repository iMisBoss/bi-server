<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:flowable="http://flowable.org/bpmn"
             targetNamespace="http://www.newtouch.com/processes">

    <process id="leaveProcess" name="请假审批流程" isExecutable="true">

        <!-- 开始节点 -->
        <startEvent id="start" name="开始"/>

        <!-- 提交申请 -->
        <userTask id="submitTask" name="提交请假申请" flowable:assignee="${startUserId}"/>

        <!-- 部门经理审批 -->
        <userTask id="managerTask" name="部门经理审批" flowable:candidateGroups="manager"/>

        <!-- 人事审批 -->
        <userTask id="hrTask" name="人事审批" flowable:candidateGroups="hr"/>

        <!-- 结束节点 -->
        <endEvent id="end" name="结束"/>

        <!-- 流程连线 -->
        <sequenceFlow id="flow1" sourceRef="start" targetRef="submitTask"/>
        <sequenceFlow id="flow2" sourceRef="submitTask" targetRef="managerTask"/>
        <sequenceFlow id="flow3" sourceRef="managerTask" targetRef="hrTask"/>
        <sequenceFlow id="flow4" sourceRef="hrTask" targetRef="end"/>

    </process>
</definitions>
